I have what I need. Key source-verified facts:

- `ReadingOrderOptions()` is instantiated empty and hardcoded in `standard_pdf_pipeline.py:506` — reading order is NOT exposed through `PdfPipelineOptions`, so failure mode 2 is largely NOT directly config-tunable at the reading-order stage. But `LayoutOptions.model_spec` (Egret L/XL) and `create_orphan_clusters`/`keep_empty_clusters` ARE the layout knobs that drive grouping.
- A full **VLM pipeline** (`VlmPipelineOptions`, Granite-Docling default, or `do_picture_description`/`do_chart_extraction`) is an entirely different lever the inline synthesis never mentions — and it directly addresses both failure modes (it reads the page as an image).
- `managed_pdfium`, `docling_parse_v2`, `docling_parse_v4` backends all exist as modules but the `PdfBackend` enum only exposes `pypdfium2` and `docling_parse`.

Now I'll write the critique.

## Critic — gaps & fixes

**1. VLM / image-pipeline lever is entirely missing — and it's the most direct fix for BOTH failure modes.** The synthesis frames the fix as "swap backend / OCR." But the installed source has a whole alternate pipeline (`VlmPipelineOptions`, `ProcessingPipeline.VLM`, default preset `granite_docling`; pipeline_options.py:1280–1321) that reads the rendered page as an image — exactly what recovers a subset-font hero number AND a visually-grouped infographic box. Also `do_picture_description` (1196) and `do_chart_extraction` (1216, "converts bar/pie/line charts into structured tabular data") target designed/infographic pages directly. None of these appear in the inline problem statement or its "PARSE layer fix." *Fix: add a VLM-pipeline candidate (and `do_chart_extraction`/`do_picture_description`) to the Thread-C matrix; it is arguably the strongest candidate for failure mode 2 and should not be omitted.*

**2. Reading-order is asserted "fixable at the parse layer" but the source says it is NOT directly config-tunable.** `standard_pdf_pipeline.py:506` hardcodes `ReadingOrderModel(options=ReadingOrderOptions())` — `ReadingOrderOptions` is never threaded through `PdfPipelineOptions`, so there is no knob to retune reading order. The only grouping-relevant knobs that ARE exposed are `LayoutOptions.model_spec` (Egret medium/large/xlarge, layout_model_specs.py:105–121) and `LayoutOptions.create_orphan_clusters` / `BaseLayoutOptions.keep_empty_clusters` (1338–1389). *Fix: the synthesis must state honestly that failure mode 2 is likely NOT reading-order-config-fixable; the testable lever is the layout model (Egret-XLarge) + orphan/empty-cluster flags, and failing those, the VLM pipeline or a different tool (0060). Right now Open Question #2 in the spec is unanswered and the inline draft leans "fix at parse layer" without naming a single config knob that touches reading order.*

**3. The candidate `PyPdfiumDocumentBackend` is stated as a fix but the source flags a regression risk against the 82%.** `pypdfium2_backend.py:174` comment: "PyPdfium2 produces very fragmented cells, with sub-word level boundaries, in many PDFs." Fragmentation is precisely the failure class the diagnostic gate buckets as "high coverage + low longest_run." *Fix: the no-regression A/B must explicitly measure `longest_run`/fragmentation on clean-text docs under PyPdfium, not just "does 27,577 decode." The draft treats the backend swap as "cheap, one-line" upside with no downside named.*

**4. "Recommended" vs "proven" conflation — multiple unverified claims stated as fact.** The inline draft asserts the garble fix "must be at the PARSE layer" and that PyPdfium/OCR will recover the numbers — but no Thread-C diff exists yet (no results file in the spike dir; only wheels + render PNGs). The #2334 community suggestions (OCR, PyPdfium) are *hypotheses from an issue thread*, not measured on our docs. *Fix: label every candidate "UNVERIFIED — pending Thread-C diff" until the diff on 7ce2c7fd / 001eb5f8 exists. The handoff doc itself warns "assumptions are making fools of us"; the draft re-commits the sin by stating the fix layer as fact.*

**5. Config knobs in pipeline_options.py the synthesis omits.** Beyond backend/OCR/TableFormer/images_scale, fidelity-relevant knobs not covered: `force_backend_text` (PdfPipelineOptions:1528 — bypass layout-model text, use embedded PDF text directly; plausibly relevant to garble/ordering), `LayoutOptions.model_spec`=Egret-XLarge, `create_orphan_clusters`, `keep_empty_clusters`, `do_cell_matching` (TableStructureOptions:124 — the comment warns it "can break table output if PDF cells are merged across columns," a no-regression hazard), `bitmap_area_threshold`/`picture_area_threshold` (gate whether infographic bitmaps even get OCR'd/described — at default 0.05 a hero-stat box may be skipped), `OcrAutoOptions` vs an explicit engine, and `do_table_structure` interaction. *Fix: Thread-A's config map must enumerate these; the draft's "do_ocr / force_full_page_ocr / images_scale / TableFormer" list is a strict subset.*

**6. Missing backends in the enumeration.** Source has `managed_pdfium_backend.py`, `docling_parse_v2_backend.py`, `docling_parse_v4_backend.py` as live modules, but `PdfBackend` enum (1013–1037) exposes ONLY `pypdfium2` and `docling_parse` (V1/V2/V4 are deprecated aliases → `DOCLING_PARSE`). *Fix: Thread-A must state that "DoclingParseV4" (what the spec/handoff says we run) is now just the `docling_parse` alias and that no separate V2/V4 selection exists via the public enum — the spec's framing "we only ever ran the default DoclingParseV4" and "try other backends like DoclingParseV2" is stale; the only real alternate public backend is `pypdfium2`. Programmatic instantiation of `ManagedPdfiumDocumentBackend` is possible but off the supported enum path.*

**7. OCR engine choice is under-specified and has a fidelity trap.** Default `ocr_options=OcrAutoOptions()` (1555) defers to whatever engine is installed (EasyOCR if GPU). `force_full_page_ocr=True` OCRs the *entire* page including already-clean text — a direct regression risk to the 82% (OCR can corrupt good embedded text). RapidOCR has a documented read-only-FS issue (PdfPipelineOptions docstring:1484). *Fix: Thread-C must pin a specific engine (EasyOCR on g6) and test `do_ocr` selectively, NOT `force_full_page_ocr` globally, in the no-regression A/B.*

**8. No-regression rule has a sampling/oracle hole.** The spec requires a stratified A/B (clean-text/table/scanned/designed) but does not (a) fix the sample size or selection method, (b) say the gate is run at the same `run_id`/prompt so only the parse varies, or (c) define the fail threshold numerically (e.g. "any drop in verbatim rate" vs "drop > X%"). The handoff also notes the gate verifies `source_snippet` verbatim against Docling text — if the backend changes, the *baseline* 82% snippets were grounded against OLD Docling serialization, so a naive re-run could show spurious regressions (the same trap that sank the pdftotext experiment, handoff line 19). *Fix: the A/B must re-extract AND re-ground both arms from their own parse output (no cross-serialization grounding), use a frozen doc list with counts per stratum, and state a hard numeric no-regression bound.*

**9. Cost estimate is hand-waved.** Open Question #3 ("unacceptable speed/cost at ~333K docs") has no number, and the inline draft gives none. Egret-XLarge layout + ACCURATE TableFormer + OCR + VLM each multiply GPU time. *Fix: Thread-C must record wall-clock/GPU-sec per doc per candidate and extrapolate to corpus scale (333K) so the recommendation is decision-ready, per Success Criterion 3.*

**10. Evidence-tag completeness.** Both evidence docs have render PNGs but only 001eb5f8 has a `-parse.txt` (the actual garbled text the LLM saw). There is NO committed parse-text artifact for 7ce2c7fd showing the "725,747"/mojibake string. *Fix: capture `evidence-7ce2c7fd-infographic-parse.txt` (the garbled glyph string) as the Thread-C before-baseline for the garble case; otherwise the success test "does 27,577 decode" has no recorded before-state to diff against.*

Minor nits: the draft says "47,155,255 IMPRESSIONS" repeats before each stat — the parse file confirms it repeats before the three email stats AND that "TAX CREDIT CALCULATOR" repeats as a separate jumble label (lines 7–16), so the grouping loss is broader than the email box alone; worth noting both mis-pairings. Also "DoclingParseV4 default" — confirm at runtime which backend the bare `DocumentConverter()` actually selects (source default is `docling_parse`); don't assert "V4" as a distinct mode.